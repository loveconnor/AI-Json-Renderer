# AI Gateway API Key (required for /api/generate endpoint)
# Get your key from https://gateway.ai.cloudflare.com or your AI provider
AI_GATEWAY_API_KEY=

# AI provider selection: "gateway" (default) or "ollama"
AI_PROVIDER=

# Optional gateway model override
AI_MODEL=

# Ollama settings (used when AI_PROVIDER=ollama)
OLLAMA_MODEL=llama3.1
# Use Ollama's OpenAI-compatible endpoint (will auto-append /v1 if missing)
OLLAMA_BASE_URL=http://localhost:11434
# Optional (Ollama accepts any non-empty key for OpenAI compatibility)
OLLAMA_API_KEY=ollama
